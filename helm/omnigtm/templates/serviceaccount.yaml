{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "omnigtm.fullname" .) }}
  labels:
    {{- include "omnigtm.labels" . | nindent 4 }}
{{- end }}
